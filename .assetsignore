# =========================================================
# Wrangler 静态资源上传忽略列表（配合 wrangler deploy）
# 发布根目录 = 仓库根时会用到这里：排除 git 内部文件、
# 系统文件、依赖与构建临时目录，避免它们被当作站点资源。
# =========================================================
.git
.wrangler
.DS_Store
Thumbs.db
node_modules
*.log
.idea
.vscode
